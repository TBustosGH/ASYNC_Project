scalar DateTime

type Comment {
    id: ID!
    post: Post!
    user: User!
    content: String!
    createdAt: DateTime!
}

type Mutation {
    createComment(parent: ID!, author: ID!, content: String!): Comment
}