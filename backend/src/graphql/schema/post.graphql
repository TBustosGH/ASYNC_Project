type Post {
    id: ID!
    user: User!
    content: String!
    comments: [Comment!]!
    created_at: String!
}

type Query {
    getAllPosts: [Post!]!
    getPost(id: ID!): Post!
}

type Mutation {
    createPost(userId: ID!, content: String!): Post
}